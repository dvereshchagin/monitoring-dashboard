# 📚 Terraform Documentation Index

Полная документация по Terraform для проекта Monitoring Dashboard.

---

## 🎯 Быстрый старт

Если вы впервые работаете с Terraform, начните здесь:

1. **[TERRAFORM_VISUAL.md](./TERRAFORM_VISUAL.md)** - Визуальный гайд ⭐ START HERE
   - Terraform за 1 минуту (схемы и диаграммы)
   - Основные концепции на картинках
   - Граф зависимостей
   - Workflow от кода до AWS

2. **[TERRAFORM_EXPLAINED.md](./TERRAFORM_EXPLAINED.md)** - Подробное объяснение
   - Что такое Terraform
   - Основные концепции (Provider, Resource, Module, State)
   - Жизненный цикл и команды
   - Примеры из проекта
   - Лучшие практики

3. **[TERRAFORM_CHEATSHEET.md](./TERRAFORM_CHEATSHEET.md)** - Шпаргалка
   - Все основные команды
   - Синтаксис
   - Функции и операторы
   - Типичные паттерны
   - Решение проблем

4. **[TERRAFORM_PROJECT_GUIDE.md](./TERRAFORM_PROJECT_GUIDE.md)** - Гайд по проекту
   - Архитектура инфраструктуры
   - Структура файлов
   - Граф зависимостей
   - Практические сценарии
   - CI/CD интеграция

---

## 📖 Документы по типу задачи

### Изучение основ
→ **TERRAFORM_EXPLAINED.md**
- Как работает Terraform?
- Что такое Provider, Resource, Module?
- Что такое State и зачем он нужен?
- Как Terraform принимает решения о создании/изменении/удалении?

### Справка по синтаксису
→ **TERRAFORM_CHEATSHEET.md**
- Как создать ресурс?
- Как использовать переменные?
- Какие есть функции?
- Как сделать условие/цикл?

### Работа с проектом
→ **TERRAFORM_PROJECT_GUIDE.md**
- Как устроен наш проект?
- В каком порядке создаются ресурсы?
- Как добавить новое окружение?
- Как масштабировать кластер?

---

## 🎓 Учебный план

### День 1: Основы (2-3 часа)
```
1. Прочитать "Что такое Terraform" в TERRAFORM_EXPLAINED.md
2. Изучить основные концепции: Provider, Resource, Variable, Output
3. Посмотреть примеры из проекта (VPC, Subnet)
4. Практика: terraform init, plan, apply в bootstrap/
```

### День 2: State и Modules (2-3 часа)
```
1. Понять работу State (TERRAFORM_EXPLAINED.md)
2. Изучить структуру модулей (TERRAFORM_PROJECT_GUIDE.md)
3. Разобрать module "network" в деталях
4. Практика: создать staging окружение
```

### День 3: Продвинутые темы (2-3 часа)
```
1. Count, for_each, условия (TERRAFORM_CHEATSHEET.md)
2. Data sources и locals
3. Зависимости между модулями
4. Практика: изменить параметры staging, сделать plan
```

### День 4: Production Ready (2-3 часа)
```
1. Backend configuration
2. Секреты и безопасность
3. CI/CD интеграция (TERRAFORM_PROJECT_GUIDE.md)
4. Практика: создать prod окружение
```

---

## 🔍 Быстрый поиск по темам

### Команды Terraform
- Основные команды → **TERRAFORM_CHEATSHEET.md** → "Основные команды"
- Debugging → **TERRAFORM_CHEATSHEET.md** → "Debugging"

### Синтаксис
- Resource → **TERRAFORM_CHEATSHEET.md** → "Resource (Создание ресурса)"
- Variables → **TERRAFORM_CHEATSHEET.md** → "Variable"
- Modules → **TERRAFORM_EXPLAINED.md** → "Модули"

### Архитектура проекта
- Структура → **TERRAFORM_PROJECT_GUIDE.md** → "Файловая структура"
- Зависимости → **TERRAFORM_PROJECT_GUIDE.md** → "Dependency Graph"
- State → **TERRAFORM_EXPLAINED.md** → "Работа с состоянием"

### Практические задачи
- Создание окружения → **TERRAFORM_PROJECT_GUIDE.md** → "Как создаётся Staging"
- Изменение ресурсов → **TERRAFORM_PROJECT_GUIDE.md** → "Практические сценарии"
- Rollback → **TERRAFORM_PROJECT_GUIDE.md** → "Откат изменений"

### Проблемы и решения
- Типичные ошибки → **TERRAFORM_CHEATSHEET.md** → "Ошибки и решения"
- Best practices → **TERRAFORM_EXPLAINED.md** → "Лучшие практики"

---

## 🚀 Практические руководства

### Как создать новое окружение (например, dev)

1. Скопировать структуру:
   ```bash
   cd infra/terraform/live
   cp -r staging dev
   ```

2. Обновить переменные в `dev/terraform.tfvars`:
   ```terraform
   environment = "dev"
   vpc_cidr = "10.1.0.0/16"  # Другой CIDR
   node_desired_size = 1      # Меньше нод для экономии
   ```

3. Обновить `dev/backend.hcl`:
   ```hcl
   key = "dev/terraform.tfstate"  # Другой путь в S3
   ```

4. Применить:
   ```bash
   cd dev
   terraform init -backend-config=backend.hcl
   terraform plan
   terraform apply
   ```

### Как добавить новый модуль

1. Создать структуру:
   ```bash
   mkdir -p infra/terraform/modules/s3
   touch infra/terraform/modules/s3/{main,variables,outputs}.tf
   ```

2. Реализовать модуль в `main.tf`

3. Использовать в окружении:
   ```terraform
   # live/staging/main.tf
   module "s3" {
     source = "../../modules/s3"
     
     bucket_name = "monitoring-staging-data"
     environment = var.environment
   }
   ```

### Как обновить версию Kubernetes

1. Изменить в `live/staging/terraform.tfvars`:
   ```terraform
   cluster_version = "1.29"  # Было 1.28
   ```

2. План покажет обновление кластера:
   ```bash
   terraform plan
   # ~ module.eks.aws_eks_cluster.main
   #   ~ version = "1.28" -> "1.29"
   ```

3. Применить (кластер обновится без downtime):
   ```bash
   terraform apply
   ```

---

## 📊 Диаграммы

### Граф зависимостей модулей

```
bootstrap
    ↓
 [S3 + DynamoDB]
    ↓
network module
    ↓
    ├──→ eks module
    │       ↓
    └──→ rds module (использует eks.security_group_id)
    
    └──→ ecr module (независимо)
```

### Жизненный цикл команды apply

```
1. Read .tf files
2. Read terraform.tfstate (current state)
3. Build dependency graph
4. Calculate diff (plan)
5. Execute API calls in correct order
6. Update terraform.tfstate
7. Show outputs
```

---

## 🔗 Внешние ресурсы

- [Официальная документация Terraform](https://developer.hashicorp.com/terraform/docs)
- [AWS Provider Documentation](https://registry.terraform.io/providers/hashicorp/aws/latest/docs)
- [Terraform Registry](https://registry.terraform.io/) - готовые модули
- [Learn Terraform](https://developer.hashicorp.com/terraform/tutorials) - интерактивные туториалы
- [Terraform Best Practices](https://www.terraform-best-practices.com/)

---

## ❓ FAQ

**Q: Terraform vs CloudFormation?**
A: Terraform мультиоблачный (AWS, GCP, Azure), CloudFormation только AWS. Terraform имеет более простой синтаксис (HCL vs JSON/YAML).

**Q: Где хранятся секреты?**
A: НЕ в коде! Используйте: переменные окружения (`TF_VAR_*`), AWS Secrets Manager, GitHub Secrets в CI/CD.

**Q: Что делать если apply зависла?**
A: Ctrl+C, затем `terraform force-unlock <LOCK_ID>` (ID будет в сообщении об ошибке).

**Q: Как откатить изменения?**
A: Вернуть код в Git к предыдущему коммиту и сделать `terraform apply`. Или восстановить старую версию state из S3.

**Q: Нужно ли коммитить .terraform/?**
A: НЕТ! Эта папка содержит скачанные провайдеры. Добавьте в `.gitignore`.

**Q: Что такое drift detection?**
A: Когда кто-то изменил инфраструктуру вручную (через консоль AWS), не через Terraform. Обнаруживается через `terraform plan` - покажет расхождения.

**Q: Можно ли использовать Terraform для Kubernetes?**
A: Да! Есть провайдер `kubernetes` и `helm`. Но для приложений лучше использовать Helm напрямую, а Terraform для инфраструктуры (VPC, EKS, RDS).

---

## 💡 Tips & Tricks

### Быстрая проверка изменений

```bash
# Показать только добавления
terraform plan | grep "will be created"

# Показать только изменения
terraform plan | grep "will be updated"

# Показать только удаления
terraform plan | grep "will be destroyed"
```

### Работа с конкретными ресурсами

```bash
# Пересоздать только RDS
terraform taint module.rds.aws_db_instance.main
terraform apply

# Обновить только network модуль
terraform apply -target=module.network
```

### Экспорт данных

```bash
# Экспорт всех outputs в JSON
terraform output -json > infra-outputs.json

# Использование в других скриптах
EKS_ENDPOINT=$(terraform output -raw eks_cluster_endpoint)
kubectl config set-cluster ... --server=$EKS_ENDPOINT
```

---

## 📝 Чеклист для Production

- [ ] Remote state настроен (S3 + DynamoDB)
- [ ] State bucket имеет versioning
- [ ] State bucket зашифрован
- [ ] Используются переменные для всех параметров
- [ ] Секреты НЕ в коде (в AWS Secrets Manager)
- [ ] Все ресурсы имеют теги
- [ ] Backend config НЕ в Git (.gitignore)
- [ ] Terraform версия зафиксирована
- [ ] Provider версия зафиксирована
- [ ] Модули документированы
- [ ] CI/CD настроен для apply
- [ ] Есть plan stage перед apply
- [ ] Есть мониторинг затрат (Cost Explorer)

---

## 🎯 Следующие шаги

1. ✅ Прочитайте **TERRAFORM_EXPLAINED.md** для понимания основ
2. ✅ Изучите **TERRAFORM_PROJECT_GUIDE.md** для работы с проектом
3. ✅ Держите **TERRAFORM_CHEATSHEET.md** под рукой как справочник
4. 🚀 Попробуйте создать тестовое окружение
5. 📚 Изучите официальную документацию для углубления знаний

---

Удачи в освоении Terraform! 🎉

_Документация создана: 2026-02-07_
_Версия Terraform: >= 1.6.0_
_AWS Provider: ~> 5.0_
